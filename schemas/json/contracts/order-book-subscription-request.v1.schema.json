{
  "$defs": {
    "Market": {
      "enum": [
        "SPOT",
        "USD_M_PERPETUAL"
      ],
      "title": "Market",
      "type": "string"
    },
    "Venue": {
      "enum": [
        "BINANCE"
      ],
      "title": "Venue",
      "type": "string"
    }
  },
  "$id": "urn:binance-market-data-contracts:order-book-subscription-request.v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Request to subscribe to an order book snapshot + diff stream.",
  "properties": {
    "depth_limit": {
      "anyOf": [
        {
          "exclusiveMinimum": 0,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "initial_snapshot_mode": {
      "const": "REQUIRED",
      "type": "string"
    },
    "market": {
      "$ref": "#/$defs/Market"
    },
    "request_id": {
      "maxLength": 128,
      "minLength": 1,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]{0,127}$",
      "type": "string"
    },
    "schema_version": {
      "const": "order-book-subscription-request.v1",
      "type": "string"
    },
    "supported_snapshot_schema_versions": {
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "type": "array"
    },
    "supported_update_schema_versions": {
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "type": "array"
    },
    "symbol": {
      "minLength": 1,
      "pattern": "^(?![\\s\\S]*[\\u0000-\\u0020\\u007F])[\\s\\S]+$",
      "type": "string"
    },
    "venue": {
      "$ref": "#/$defs/Venue"
    }
  },
  "required": [
    "request_id",
    "schema_version",
    "venue",
    "market",
    "symbol",
    "initial_snapshot_mode",
    "supported_snapshot_schema_versions",
    "supported_update_schema_versions"
  ],
  "type": "object"
}
